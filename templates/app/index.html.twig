{% extends 'base.html.twig' %}

{% block title %} AppController!{% endblock %}

{% block body %}
	
	<div class="jumbotron">
	  <h1 class="display-4">Hello, {% if(is_granted('IS_AUTHENTICATED_FULLY')) %} {{ app.user.username }}{% endif %}</h1>
	  <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
	  
	  {% if (is_granted('ROLE_ADMIN')) %}
	  <a class="btn btn-primary" href="{{path('easyadmin')}}">Administration</a>
	  {% endif %}	
	</div>

{% endblock %}
